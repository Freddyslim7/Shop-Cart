##This is a Shopping cart project using HTML, CSS, and Javascript. The objective was to create a cart that adds and removes items, as well as calculate the total price at the checkout of the cart. The (base) was built with html, (structure) was styled and designed with CSS, finally the interactivity was achieved with Javascript.